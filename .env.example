# Copy to .env and adjust. Do not commit real secrets.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change-me
POSTGRES_DB=devexp

JWT_SECRET=devexp-change-me-use-at-least-32-chars!!
JWT_EXPIRATION_MS=3600000

CORS_ORIGINS=*
